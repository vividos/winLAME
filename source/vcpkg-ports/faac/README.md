# speex port

This is an overlay port of the [faac](https://github.com/knik0/faac/) library.
There is no original port in vcpkg, so this builds the library using `meson`.
Only windows triplets are supported.
